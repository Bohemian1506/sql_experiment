# render.yaml
services:
  - type: web
    name: rails8-sqlite-blog
    runtime: docker
    plan: free
    autoDeploy: false
    disk:
      name: sqlite-storage
      mountPath: /app/storage
      sizeGB: 1